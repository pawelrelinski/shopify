import { NotificationDirective } from './notification.directive';

describe('NotificationDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationDirective();
    expect(directive).toBeTruthy();
  });
});
